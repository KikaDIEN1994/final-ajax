<h1> <%= @email.object %></h1>

<p><%= @email.body %></p>

<button type="button" class="btn btn-dark"><a class="text-white"
<%= link_to 'Supprimer', email_path(@email.id),
                         method: :delete, remote: true %>  </button>
